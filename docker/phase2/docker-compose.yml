services:
  backend:
    build: ./backend
    ports:
      - 8000:5000
    environment:
      - PORT=7000
  frontend:
    build: ./frontend
    ports:
      - 5173:5173

  redis:
    image: redis
    ports:
      - 6379:6379